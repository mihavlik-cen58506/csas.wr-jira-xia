[project]
name = "app-xia-nightly-processor"
dynamic = ["version"]
description = "XIA Nightly Processor — bulk-moves eligible Jira issues to the XIA project nightly."
authors = [
    { name = "Michal Havlík", email = "mihavlik@csas.cz" },
]
readme = "README.md"
requires-python = "~=3.14.0"
dependencies = [
    "keboola-component>=1.10.0",
    "keboola-http-client>=1.0.1",
    "keboola-utils>=1.1.0",
    "pydantic>=2.11.7",
]

[dependency-groups]
dev = [
    "freezegun>=1.5.1",
    "keboola.datadirtest>=2.2.0",
    "mock>=5.2.0",
    "pre-commit>=4.5.0",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
    "ty>=0.0.24",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
extend-select = [
    "I",   # isort – import sorting
    "UP",  # pyupgrade – modern Python syntax
    "G",   # flake8-logging-format – no f-strings / str.format in logging calls
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ty.environment]
extra-paths = ["src"]
